package es.ua.dlsi.im3.omr.encoding.agnostic;

/**
 * @autor drizo
 */
public enum AgnosticVersion {
    /**
     * v1 Uses barline, tabs for separation of elements, does not include vertical stacking of elements.
     * This is the one presented to Calvo and Rizo Applied Sciences and ISMIR 2018
     */
    v1,
    /**
     * Used for HISPAMUS Project
     */
    v2
}
